# V4 Admission Representative-Crawl Remediation Plan

Status: `APPROVED` by the user; execute U1-U5 continuously with checker gates.

## Locked outcome

Repair the false-limited V4 admission boundary proven by report
`010afe2d-231e-4488-a8b3-eeb281595a88`, deploy one exact-SHA protected-Staging
runtime, then create and accept one replacement paid V4 report for
`https://shun-express.com/`. Do not repair or pay the failed report.

## U1 — Preserve and reproduce the failure

1. Record the terminal free/admission/snapshot identities, counts, exclusion
   reasons, zero commerce, checkout rejection boundary, and production freeze.
2. Add a deterministic Shun-shaped test fixture before changing behavior.
3. Prove current code expands equivalent candidates and maps any exclusion to
   `completed_limited`.
4. Executor returns a real diff/test report; independent checker rereads the
   design and scope and returns one control verdict.

## U2 — Bound the representative candidate frontier

1. Make initial and dynamic discovery share robots and HTML eligibility.
2. Collapse same-site apex/`www` and equivalent recursive query identities
   without collapsing genuinely distinct paths or bodies.
3. Use the existing page-type/template representative policy to keep work
   finite while preserving replacement candidates and the 51st unique-body
   custom-service boundary.
4. Preserve explicit provenance for ignored policy/duplicate candidates.
5. Run focused site-crawler/crawler/collector tests and audit the complete U2
   diff against its file/budget allowlist.

## U3 — Correct terminal status semantics

1. Keep exact-body deduplication and immutable duplicate exclusions.
2. Return `completed` only when the eligible representative frontier is
   exhausted with 1-50 unique analyzable bodies.
3. Keep deadline residue and unresolved transient reads fail-closed as
   `completed_limited`; keep zero evidence `unavailable` and 51 unique bodies
   `custom_service`.
4. Run admission runtime/production, commercial-order, PostgreSQL acceptance,
   traceability, matrix, lint, build, full tests, DB audit, and diff checks.
5. Independent checker must be `CONFORMANT` before any image or Preview action.

## U4 — Exact-SHA protected-Staging runtime

1. Commit the conformant code/docs, build one full-SHA Staging image, and
   create one Preview from the same SHA.
2. Align alias, image, runtime/deployment marker, database marker, and both
   persistent Worker presences; verify zero claimable/recoverable jobs.
3. Remove only named unreferenced superseded Staging test images.
4. Compare production deployment and all production container identities to
   the frozen before snapshot. Independent checker gates U5.

## U5 — One replacement paid report

1. Browser-submit one Chinese forced-fresh Shun report and record the new
   report/free/admission/snapshot tuple.
2. Require automatic claims and strict completed admission within the locked
   deadlines. Verify unique bodies, immutable snapshot, and LingShun identity.
3. Persist the three already locked questions; create and complete one CNY 199
   Airwallex Sandbox checkout/payment through the signed Webhook only.
4. Require the signed Webhook transaction to create exactly one entitlement,
   reserved credit, Core job, Core dispatch, and unsent `payment_confirmed`
   intent. Successful Core must create exactly one exact-target token and one
   unsent `report_ready` intent; Core and diagnosis must both fully complete.
5. Verify three substantive Chinese answers, no SF identity, at most five
   sources per answer, authorized HTML HTTP 200 and visible desktop/mobile.
6. Run final DB/identity/production/diff audits, update durable docs/evidence,
   push the branch, and do not merge or deploy production.

This one replacement is the final authorized attempt. Any nonconforming
admission or commercial terminal state stops the run without any later report,
later payment, replay, or historical mutation.
